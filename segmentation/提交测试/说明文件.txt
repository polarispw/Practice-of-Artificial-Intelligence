1.pycharm 打开编译器会自动提示根据requirements.txt安装
  也可以命令行创建python环境以后用如下命令 pip install -r requirements.txt
  根据老师发的列表，环境目前都是齐全的

2.进入虚拟环境运行
  python test_2stage.py --data-path ./Image 
			# (数据集路径，不输就默认和此脚本同级平行）
  
3.程序会新建Label文件夹按照数据集格式保存运行结果